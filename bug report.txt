- In the gui, when clicking on a node to open its pop-up, the pop-up only closes after hitting the add button for the visaulizer. If one clicks outside, the wrokflow breaks and the window gets unresponsive. Consider to add an (x) button to close the pop-up or close it autimatcially if one cicks outside the popup.

- In the gui, sometimes creating a visualizer and dragging it to the side gives the following error:
  File "/Users/timdick/Documents/Software Development/briann/src/briann/python/GUI/model_explorer.py", line 403, in _on_drag_motion
    dx = self._initial_x + event.x - self._mouse_x
                                     ^^^^^^^^^^^^^
AttributeError: 'Area' object has no attribute '_mouse_x'

- In GUI, Flip the spectorgram in the heatmap display?

- In GUI, Fix the next stimulus button

- Check how scheduler updates the areas and what if we merge areas?

- Try to integrate SNN Torch so that we can easily swop between spiking and continous mode

- In brian step, sort the areas by index to make forward prop immediate

- In brian step, move the collect inputs and forward methods into a signle loop, this trades paralelizability with efficiency in terms of steps needed to propagate a signal through the network

- IMPROTANT: Fix weight loading

- IMPROTANT: Add gating mechanism to time frame accumulator